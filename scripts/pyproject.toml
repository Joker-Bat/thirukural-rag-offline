[project]
name = "kural-rag-scripts"
version = "0.1.0"
description = "Thirukkural dataset ingestion, normalization, and multilingual embedding generator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sentence-transformers>=3.0.0",
    "numpy>=1.24.0",
    "requests>=2.31.0"
]
